import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Greenvoltt - Advancing The Circular Economy',
  description: 'Greenvoltt is the Premiere Circular Economy Entity operating out of the Caribbean Region. We purchase scrap batteries and advance environmental sustainability.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}